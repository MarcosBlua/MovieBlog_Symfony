{% extends 'base.html.twig' %}

{% block body %}
{{parent()}} 

{#<div class="header" style="text-align:center; background-color: #CECECE; height: auto;">
	<br>
	
    <img src={{app.user.imagenPerfil}} width="170px" heigth="170px" style="border-radius: 100%;"/>
    <h1 style="text-align:center"> <b> {{usuario.username}} </b> </h1>

	<br>
</div>#}

{% if app.user.username == formulario.username.vars.value %}
    <br>

<div class="container forma" style="width: 80%;">
    <br>

        <h1 style="text-align:center"> Modifique sus datos </h1>

        <br>

        {{form_start(formulario)}}

        <div class="form-group">
            <label> Usuario: </label>

            {{form_widget(formulario.username, { 'attr': {'class': 'form-control'} })}}
        </div>

        <div class="form-group">
            <label> Contraseña: </label>

            {{form_widget(formulario.password, { 'attr': {'class': 'form-control'} })}}            
        </div>

        <div class="form-group">
            <label> Imagen de perfil actual: </label>

            <img src={{formulario.imagenPerfil.vars.value}} width="80px" heigth="80px" style="border-radius: 100%;"/>

            {{form_widget(formulario.imagenPerfil, { 'attr': {'class': 'form-control'} })}}
        </div>

        {{form_end(formulario)}}

        <br>
</div>

<br>

{% else %}
    <br>

    <div class="container forma">

        <h1 style="color: #CA1212;"> No puede modificar los datos de otro usuario </h1>

    </div>

    <br>
{% endif %}

{% endblock %}